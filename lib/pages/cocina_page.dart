// cocina_page.dart
import 'package:flutter/material.dart';

class CocinaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Página Cocina')),
      body: Center(child: Text('Contenido de la cocina')),
    );
  }
}

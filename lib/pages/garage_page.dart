// garage_page.dart
import 'package:flutter/material.dart';

class GaragePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Página Garage')),
      body: Center(child: Text('Contenido del garage')),
    );
  }
}

// sala_page.dart
import 'package:flutter/material.dart';

class SalaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Página Sala')),
      body: Center(child: Text('Contenido de la sala')),
    );
  }
}
